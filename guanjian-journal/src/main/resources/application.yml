server:
  port: 8080
spring:
  application:
    name: guanjianjournal
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/theguanjianjournal?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: abc123

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.lynssen.guanjianjournal.dao
  #彻底省去编写冗长的全限定类名（包名 + 类名），直接使用类名即可，如之前在xml文件中需要指定包名 + 类名，现在只需要指定类名即可
  configuration:
    map-underscore-to-camel-case: true


